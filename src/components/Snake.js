import React, {Component} from "react";

class Snake extends Component{	

	render(){
	  const {grid, snakeDirection, putSnakeOnGrid} = this.props;
	  return(	
	    <div>
		</div>
	  );	
	}
} 

export default Snake;